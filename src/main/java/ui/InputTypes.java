package ui;


public enum InputTypes {
    FLIGHT_NO,
    BOOKING_NO,
    DATE,

    NAME,
    SURNAME,
    PASSWORD,
    REGISTERED_PASSWORD,
    USERNAME,
    REGISTERED_USERNAME,
    EMAIL,
    REGISTERED_EMAIL,

    CITY,
    INTEGER,
    EXIT
}